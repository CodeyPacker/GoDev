A Go app to quickly startup one of your projects while also opening a handful of URLs. 
1. Add your projects to the config.json
2. Adding additional urls for the browser to open is fairly straightforward in the browserControl.go file, just add them there as well as the config.json
3. run `go run *.go` at the root to start the process
